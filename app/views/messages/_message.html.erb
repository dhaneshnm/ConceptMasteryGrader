<div class="flex <%= message.from_user? ? 'justify-end' : 'justify-start' %>" id="<%= dom_id(message) %>">
  <div class="max-w-xs lg:max-w-md">
    <div class="<%= message_bubble_class(message) %> rounded-lg px-4 py-2">
      <p class="text-sm"><%= simple_format(message.content, class: "mb-0") %></p>
    </div>
    <div class="text-xs text-gray-500 mt-1 <%= message.from_user? ? 'text-right' : 'text-left' %>">
      <%= message_author_name(message) %> • <%= time_ago_in_words(message.created_at) %> ago
    </div>
  </div>
</div>